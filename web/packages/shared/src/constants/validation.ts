/** Maximum number of characters for a display name or short label. Matches backend MaxDisplayNameLen. */
export const MAX_DISPLAY_NAME_LEN = 64;

/** Soft warning threshold for display name length — triggers UX warning before hard limit. */
export const MAX_DISPLAY_NAME_SOFT_LEN = 50;

/** Maximum number of characters for a description field. Matches backend MaxDescriptionLen. */
export const MAX_DESCRIPTION_LEN = 128;
